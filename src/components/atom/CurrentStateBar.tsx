import styled from '@emotion/styled';

const CurrentStateBar = () => {
  return <CurrentState />;
};

export default CurrentStateBar;

const CurrentState = styled.div`
  width: 140px;
  height: 5px;

  background-color: #71387a;
`;
